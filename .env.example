DATABASE_URL=postgresql://pitantir:pitantir@localhost:5432/pitantir

# For the Next.js dev server, copy PITPANDA_API_KEY to apps/web/.env.local
PITPANDA_API_KEY=
ITEM_DATA_PROVIDER=pitpanda

# Worker (apps/worker)
# WORKER_ID=worker-1
# WORKER_POLL_MS=2000
# JOB_LEASE_MS=60000
# INVENTORY_SOURCE=mock
